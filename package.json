{
  "name": "applyflow",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd ../server && npm install",
    "start": "node server/db/migrate.js && node server/index.js"
  }
}
